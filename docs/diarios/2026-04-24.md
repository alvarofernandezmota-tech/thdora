# Diario thdora — 24 abril 2026

## Resumen

Primera puesta en producción de thdora en servidor real (Ubuntu PC grande). El bot corre 24/7 con Docker y `restart: always`.

---

## Trabajo realizado

### Fixes para arrancar en Docker

| Archivo | Problema | Solución |
|---|---|---|
| `requirements.txt` | `backports.asyncio.runner==1.2.0` no existe para Python 3.12 | Eliminado |
| `docker-compose.yml` | Puerto 8000 ocupado por LiteLLM (ai-toolkit) | Cambiado a 8001 |
| `docker-compose.yml` | Healthcheck apuntaba a `/health` (404) | Corregido a `/` |
| `docker/entrypoint-bot.sh` | Esperaba `/health` para arrancar | Corregido a `/` |

### Estado final
```
✔ thdora-api   Up (healthy)   0.0.0.0:8001->8000
✔ thdora-bot   Up             corriendo
```

---

## Estado del bot

### ✅ Funciona
- Bot conectado a Telegram y respondiendo
- Menú de botones (citas, hábitos) operativo
- Scheduler: recordatorio 08:00 y 22:00 programados
- `restart: always` → se levanta solo si el PC se reinicia o el bot peta

### ❌ Pendiente
- API key Groq inválida en `.env` → NLP (texto libre) da error 401
- El menú de botones funciona sin Groq, pero los comandos en lenguaje natural no

---

## Próximos pasos

1. Arreglar key Groq en `.env` del servidor → `docker compose restart bot`
2. Probar flujo completo: citas + hábitos + NLP
3. Valorar migrar NLP a Ollama local (sin key, sin límites)
4. Instalar Tailscale → SSH desde Acer en cualquier lugar
5. Script auto-update para despliegues futuros
