# Diario — 14 junio 2026

## Resumen de sesión

Sesión de migración y reactivación de THDORA. El bot llevaba parado desde el 31 mayo 2026. Hoy se ha migrado del servidor Acer al servidor Madre (workstation principal, Omarchy). Primera build Docker en Madre en curso al cerrar esta entrada.

---

## ✅ Hecho hoy

- Acceso físico a Madre resuelto — `sshd` activado con `systemctl enable --now sshd`
- Repo clonada en `~/dev/thdora` en Madre
- `.env` configurado con `TELEGRAM_BOT_TOKEN` y `GROQ_API_KEY` nueva
- `GROQ_API_KEY` renovada en [console.groq.com](https://console.groq.com) — NLP Toki desbloqueado
- Verificado que `THDORA_API_URL` no va en `.env` — definida en `docker-compose.yml` como `http://api:8000`
- Editor en Omarchy: `nvim` (`nano` no disponible)
- `git pull origin main` ejecutado — local sincronizado con GitHub
- `docker compose up -d` lanzado — build en curso (primera vez, ~5 min)
- `COMO_PROCEDER.md` actualizado — rutas, Docker, tareas completadas
- Diario de sesión creado en `docs/diarios/2026-06-14.md`
- Setup de Madre documentado en `docs/setup/madre-deploy.md`

---

## 🖥️ Servidor destino — Madre

| Campo | Valor |
|---|---|
| Hostname | Madre |
| OS | Omarchy (Arch Linux + Hyprland + Wayland) |
| CPU | Intel i5-8400 |
| RAM | 16 GB |
| GPU | GTX 1060 6GB |
| IP Tailscale | `100.91.112.32` |
| Path repo | `~/dev/thdora` |
| Gestor contenedores | Docker + docker compose |

---

## 📦 Deploy

```bash
cd ~/dev/thdora
cp .env.example .env
nvim .env   # añadir TELEGRAM_BOT_TOKEN + GROQ_API_KEY
git pull origin main
docker compose up -d
docker compose ps
docker compose logs -f
```

---

## 💡 Decisiones tomadas

- **Servidor:** THDORA migra de Acer → Madre permanentemente. Madre tiene más potencia (GTX 1060) y es el workstation principal.
- **URL interna:** `THDORA_API_URL=http://api:8000` — sin exposición pública, acceso vía Tailscale.
- **Editor:** nvim en Omarchy. `nano` no disponible.
- **Repo:** estructura `docs/` a reestructurar — diarios y sesiones van a `.github/`, solo docs técnica pública en `docs/`. Pendiente de ejecutar.
- **personal-v2:** queda como repo de conocimiento puro — sin código.
- **Ecosistema IA:** Perplexity gestiona GitHub + docs. Gemini para voz/visual/Google.

---

## 🔶 Pendiente inmediato

- [ ] Verificar `docker compose ps` — thdora-api y thdora-bot Running
- [ ] Probar `/start` en Telegram
- [ ] Probar texto libre — NLP Toki con Groq key nueva
- [ ] Ejecutar checklist de producción del ROADMAP

## 🔷 Pendiente próxima sesión

- [ ] Reestructurar `docs/` — mover diarios/sesiones/auditoria a `.github/`
- [ ] Actualizar ROADMAP.md con estado real post-migración
- [ ] Bloque 1.2 — mostrar horario disponible antes de mover cita
- [ ] BUG 1 y BUG 2 — borrar/editar citas
