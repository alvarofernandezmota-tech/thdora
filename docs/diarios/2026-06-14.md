# Diario — 14 junio 2026

## Resumen de sesión

Sesión de migración y reactivación de THDORA. El bot llevaba parado desde el 31 mayo 2026. Hoy se ha migrado del servidor Acer al servidor Madre (workstation principal, Omarchy). Build Docker completada y bot corriendo en producción al cierre de sesión.

---

## ✅ Hecho hoy

- Acceso físico a Madre resuelto — `sshd` activado con `systemctl enable --now sshd`
- Repo clonada en `~/dev/thdora` en Madre
- `.env` configurado con `TELEGRAM_BOT_TOKEN` y `GROQ_API_KEY` nueva
- `GROQ_API_KEY` renovada en [console.groq.com](https://console.groq.com)
- Verificado que `THDORA_API_URL` no va en `.env` — definida en `docker-compose.yml` como `http://api:8000`
- `git pull origin main` — local sincronizado con GitHub
- `docker compose up -d` — build completada (121s), ambos contenedores levantados
- `thdora-api` → Healthy ✅
- `thdora-bot` → Started ✅
- Bot responde en Telegram — menú de hábitos y citas funcional ✅
- `COMO_PROCEDER.md` actualizado — rutas, Docker, tareas completadas
- Diario de sesión creado y cerrado
- Setup de Madre documentado en `docs/setup/madre-deploy.md`

---

## 🖥️ Servidor destino — Madre

| Campo | Valor |
|---|---|
| Hostname | Madre |
| OS | Omarchy (Arch Linux + Hyprland + Wayland) |
| CPU | Intel i5-8400 |
| RAM | 16 GB |
| GPU | GTX 1060 6GB |
| IP Tailscale | `100.91.112.32` |
| Path repo | `~/dev/thdora` |
| Gestor contenedores | Docker + docker compose |

---

## 📦 Deploy

```bash
cd ~/dev/thdora
nvim .env   # añadir TELEGRAM_BOT_TOKEN + GROQ_API_KEY
git pull origin main
docker compose up -d
docker compose ps
docker compose logs -f
```

---

## 💡 Decisiones tomadas

- **Servidor:** THDORA migra de Acer → Madre permanentemente
- **URL interna:** `THDORA_API_URL=http://api:8000` — sin exposición pública, acceso vía Tailscale
- **Editor:** nvim en Omarchy — `nano` no disponible
- **Repo:** estructura `docs/` a reestructurar — diarios/sesiones/auditoria van a `.github/`, solo docs técnica pública en `docs/`. Pendiente próxima sesión
- **Ecosistema IA:** Perplexity gestiona GitHub + docs. Gemini para voz/visual/Google

---

## 🔍 Observaciones del bot

- **Menú de hábitos y citas** → funciona sin Groq, va por botones. Robusto ✅
- **Modo Toki (texto libre)** → depende de Groq. Pendiente verificar que responde con key nueva
- El bot actualmente está "pillado" en el menú — flujo normal, no es un bug
- Los bugs conocidos (BUG 1, BUG 2, BUG 3) no bloquean el uso diario

---

## ✅ Completado hoy

- [x] Groq key renovada
- [x] Migración a Madre
- [x] Docker build completada
- [x] Bot corriendo — `thdora-api` Healthy, `thdora-bot` Started
- [x] Bot responde en Telegram (menú funcional)
- [x] Documentación actualizada (diario, setup, COMO_PROCEDER)

## 🔷 Pendiente próxima sesión

- [ ] Verificar modo Toki — escribir texto libre y comprobar que Groq responde
- [ ] Reestructurar `docs/` — mover diarios/sesiones/auditoria a `.github/`
- [ ] Actualizar ROADMAP.md con estado real post-migración
- [ ] Bloque 1.2 — mostrar horario disponible antes de mover cita
- [ ] BUG 1 — borrar cita por fecha futura
- [ ] BUG 2 — editar cita con intent genérico
