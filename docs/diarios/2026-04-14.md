# 🗓️ Diario de sesión — 14 abril 2026

---

## Sesión mañana — 12:00 CEST

**Versiones:** v0.12.0 → v0.13.0
**Estado al cerrar:** ✅ Bot listo para pruebas en vivo

### ✅ Lo que se hizo

#### 1. Auditoría completa del sistema

**Problemas UX identificados y corregidos:**

| ID | Problema | Solución | Commit |
|----|----------|----------|--------|
| UX-1 | Sin feedback visual mientras Groq procesa | `⏳ Procesando...` + delete automático | v0.13 |
| UX-2 | `/start` no explicaba el modo lenguaje natural | Añadida `_AYUDA_NLP` con ejemplos | v0.13 |
| UX-3 | Contexto NLP se perdía al reiniciar | `PicklePersistence` activa | v0.13 |
| UX-4 | Hora `00:00` sin aviso | Confirmación explícita antes de guardar | v0.13 |
| PERS-1 | Personalidad sin documentar | `_CHAT_SYSTEM` refinado + docstring | v0.13 |

**Decisión: NO integrar thea-ia con THDORA**
- THDORA resuelve el mismo problema con menos complejidad
- NLP de Groq más potente que los extractores propios de thea-ia
- thea-ia queda como laboratorio de ideas / referencia futura

#### 2. v0.12.0 — NLP con Groq (F13-base)
- `groq_router.py` completo: clasificador + extractor + chat libre
- Memoria conversacional en `user_data["nlp_history"]` (10 mensajes)
- `docs/NLP_ARQUITECTURA.md` creado

#### 3. v0.13.0 — UX + PicklePersistence + Personalidad
- `⏳ Procesando...` antes de llamar a Groq
- Fix hora `00:00` → confirmación antes de crear
- `PicklePersistence` activa en `main.py` v4.1
- `_CHAT_SYSTEM` reescrito: 2 frases máx, tono directo

### 💬 Decisiones

| Decisión | Razón |
|---|---|
| PicklePersistence en lugar de Redis | Más simple, sin dependencias extra |
| Fix 00:00 en nlp.py, no en groq_router | La lógica de negocio va en el handler |

_Cerrada: 14 abril 2026 — 12:35 CEST_

---

## Sesión tarde — 16:00–19:10 CEST

**Versiones:** v0.13.0 → v0.15.2
**Rama:** `main`

### ✅ Lo que se hizo

#### v0.14.0 — Modo Toki: contexto real
- `api_context` inyectado en el system prompt de Groq
- `_get_api_context` — 3 llamadas en paralelo con `asyncio.gather`
- Intent `desconocido` → menú del bot, no texto inventado
- Probado en vivo: `¿qué tengo hoy?` responde con datos reales ✅

#### v0.15.0 — Calidad y rendimiento
- **Solapamiento real en API** — `check_conflict` con bloques de 60 min (antes era hora exacta)
- **Horario visual** — `_build_day_schedule` franjas 30 min con 🔴/🟢/⚠️
- **Vista semana ×7 más rápida** — 14 llamadas HTTP en serie → 2 concurrentes con `asyncio.gather`
- **32 tests nuevos** — 18 solapamiento API + 14 horario visual
- **README portfolio-ready** — badges, stack, arquitectura, decisiones de diseño

#### v0.15.1 — Fix conflicto cita
- `_check_and_show_conflict` helper centralizado
- Editar hora ahora también comprueba solapamiento (antes no lo hacía)
- Mensaje de conflicto con nombre + rango de la cita bloqueante

#### v0.15.2 — NLP v2 + cierre
- **Cache TTL 2 min** — API solo se llama si han pasado >120s. Invalidación automática
- **Contexto semana completa** — hoy + mañana + semana entera para borrar/editar
- **`nlp_disambig.py` nuevo** — botones inline cuando hay varias citas candidatas
- **`main.py` v4.2** — `cb_nlp_disambig` registrado
- **`.gitignore` corregido** — `data/` completo protegido (`thdora.db` no estaba)
- **ROADMAP** — 4 bloques priorizados con tabla de estado
- **Open source confirmado** — código público, datos y tokens protegidos

### 💬 Decisiones

| Decisión | Motivo |
|---|---|
| Cache TTL en vez de invalidación manual total | Balance frescura de datos vs llamadas API |
| Contexto semana completa en desambiguación | El modelo necesita ver todas las citas |
| Botones inline para desambiguar | Mejor UX que pedir nombre exacto por texto |
| `data/` completo en `.gitignore` | DB con datos personales en repo público = riesgo |
| Repo open source | El código no es el valor, los datos sí |

### ⏩ Siguiente sesión (15 abril 2026)
1. **Mañana** — Ejercicios Musk de Python
2. **Tarde** — Bloque 1.2: slots disponibles al mover una cita

### 📊 Estado al cierre
```
Bot v4.2 + API 14 endpoints + NLP Groq modo Toki
SQLite persistente + Scheduler F12 + 32 tests
Documentación: CHANGELOG · ROADMAP · ARCHITECTURE · diarios/
```

_Cerrada: 14 abril 2026 — 19:10 CEST_
