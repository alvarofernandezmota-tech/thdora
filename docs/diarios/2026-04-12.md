# 📓 Diario — 12 abril 2026

> **Sesión:** noche (21:00–21:20 CEST)  
> **Estado al iniciar:** v0.10.0 — handlers.py monolítico pendiente de refactorizar  
> **Estado al cerrar:** v0.11.0 — bot modular + tests + Docker listo  

---

## ✅ Lo que se hizo hoy

### 1. F9.6 — Refactor handlers.py monolítico

Eliminado `src/bot/handlers.py` (60KB, ~1500 líneas) y dividido en package limpio:

```
src/bot/
├── keyboards.py         ← todos los _kb_* + HABIT_TYPE_EMOJIS + FRANJAS
├── utils/
│   ├── dates.py           ← _parse_date_flex, _date_label, _date_short, _greeting, _monday
│   └── accum.py           ← _accumulate_value, _clean_acum_context
└── handlers/
    ├── __init__.py        ← re-exporta todo (main.py sin cambios)
    ├── menu.py            ← /start, cb_menu_home, cb_quick_dispatch
    ├── citas.py           ← /citas, /nueva + FRANJAS HORARIAS
    ├── habitos.py         ← /habitos, /habito, borrar/editar/sumar
    ├── semana.py          ← /semana, nav semanal
    ├── config.py          ← /config
    └── common.py          ← /cancelar, /resumen, error_handler
```

**También incluido en F9.6:** flujo `/nueva` con franjas horarias:
- `NUEVA_FRANJA` → `NUEVA_HORA_PUNTO` → `NUEVA_HORA_CUARTO` → `NUEVA_NOMBRE`
- Opción `✏️ Exacta` siempre disponible en cualquier paso
- Vuelta a `NUEVA_FRANJA` si hay conflicto de hora (no a texto libre)

### 2. Suite de tests para el bot (72 casos nuevos)

| Fichero | Nº tests | Cubre |
|---------|----------|-------|
| `tests/unit/bot/test_dates.py` | 18 | utils/dates.py |
| `tests/unit/bot/test_accum.py` | 12 | utils/accum.py |
| `tests/unit/bot/test_keyboards.py` | 14 | keyboards.py + franjas |
| `tests/bot/test_handlers_citas.py` | 22 | /nueva paso a paso + error API |
| `tests/bot/test_handlers_habitos.py` | 18 | /habito + conflicto + sumar |
| `tests/bot/test_handlers_menu.py` | 8 | /start + dispatch |

Todos usan `AsyncMock` + `@patch("api")` — sin Telegram real, sin API real.
`pytest.ini` actualizado con `asyncio_mode = auto`.

### 3. F9.7 — Docker + despliegue 24/7

Ficheros creados:
- `Dockerfile` — multi-stage Python 3.12-slim, usuario no-root
- `docker-compose.yml` — servicios `api` + `bot`, volumen `thdora_data`, `restart: always`
- `docker/entrypoint-api.sh` — init DB → uvicorn producción
- `docker/entrypoint-bot.sh` — espera API healthy (30 reintentos) → arranca bot
- `docker/.env.docker.example` — plantilla de variables
- `.dockerignore` — excluye tests, data/*.db, .env, cache
- `Makefile` — nuevos targets `docker-build`, `docker-up`, `docker-down`, `docker-logs`, `docker-db`, `docker-shell-api`, `docker-rebuild`, `test-bot`

**Clave de diseño:** `bot` tiene `depends_on: api: condition: service_healthy`.
No arranca hasta que `/health` devuelve 200.

### 4. CHANGELOG y ROADMAP

- `CHANGELOG.md` → v0.11.0 documentado
- `ROADMAP.md` → F9.6 y F9.7 marcadas como ✅, checklist prueba en vivo actualizado

---

## ⚠️ Decisión importante: F9.8 bloqueada hasta prueba en vivo

**F9.8 (multi-usuario) NO se implementa todavía.**

Motivo: el multi-usuario toca tablas SQLite, todos los routers de la API, y el bot al mismo tiempo.
Si hay bugs sin descubrir en el menú actual y añadimos `user_id` por todas partes,
el debugging se complica muchisimo. Primero validar la base.

---

## 📋 Checklist de pruebas en vivo (hacer ANTES de F9.8)

> Ejecutar con `make run-api` + `make run-bot` en local,  
> o con `make docker-build && make docker-up` si Docker está instalado.

### Comandos básicos
- [ ] `/start` — aparece menú con botones inline + saludo según hora
- [ ] `/cancelar` — cancela cualquier flujo en curso
- [ ] `/resumen` — muestra citas + hábitos del día

### Citas — navegación
- [ ] `/citas` — muestra citas de hoy
- [ ] Botón `◀️` — va al día anterior
- [ ] Botón `▶️` — va al día siguiente
- [ ] Botón central `[Sáb 12 abr]` — muestra fecha real
- [ ] Botón `🏠 Menú` — vuelve al menú principal
- [ ] Botón `📋 Semana` — va a vista semanal
- [ ] Botón `💤 Hábitos` — cambia a vista hábitos

### Citas — /nueva con franjas
- [ ] Botón `➕ Nueva` desde vista `/citas` — arranca el flujo
- [ ] Paso 1: fecha con `hoy` → avanza
- [ ] Paso 1: fecha con `mañana` → avanza
- [ ] Paso 1: fecha inválida → pide de nuevo
- [ ] Paso 2: botón `🌅 Mañana` → muestra horas 06-13
- [ ] Paso 2: botón `🌆 Tarde` → muestra horas 14-21
- [ ] Paso 2: botón `🌙 Noche` → muestra horas 22-05
- [ ] Paso 2: botón `✏️ Exacta` → pide HH:MM directamente
- [ ] Paso 3: seleccionar hora en punto → muestra cuartos
- [ ] Paso 3: `🕐 Ver cuartos` → muestra cuartos
- [ ] Paso 3: `✏️ Exacta` → pide HH:MM
- [ ] Paso 4: seleccionar cuarto (ej. 10:30) → avanza a nombre
- [ ] Paso 4: `✏️ Exacta` → pide HH:MM
- [ ] HH:MM válida (10:30) → avanza a nombre
- [ ] HH:MM inválida (99:99) → pide de nuevo
- [ ] Nombre + tipo + notas (ó /skip) → cita guardada en DB
- [ ] Conflicto de hora → aviso ⚠️ + vuelve a franjas

### Citas — vista detalle y acciones
- [ ] Click en ⏰ hora de una cita → vista detalle
- [ ] Editar cita → flujo edición funciona
- [ ] Borrar cita → pide confirmación → se borra

### Hábitos
- [ ] `/habitos` — muestra hábitos de hoy
- [ ] Nav ◀️▶️ igual que en citas
- [ ] Botón `📊 Citas` — cambia a vista citas
- [ ] `➕ Nuevo` desde vista hábitos → arranca flujo
- [ ] `/habito` — nombre libre → valor → guardado
- [ ] Valor `+N` → acumula sobre el existente
- [ ] Valor existente sin `+` → muestra conflicto (Sobreescribir / Sumar / Cancelar)
- [ ] Editar nombre del hábito
- [ ] Editar valor del hábito
- [ ] Borrar hábito
- [ ] `➕` en hábito existente (botón sumar rápido)

### Semana
- [ ] `/semana` — muestra semana actual
- [ ] Nav ◀️▶️ entre semanas
- [ ] Click en día → va a vista citas de ese día

### Config
- [ ] `/config` — muestra tipos de hábitos configurados
- [ ] Añadir tipo con botones rápidos

### Docker (opcional si no tienes Docker instalado)
- [ ] `make docker-build` — construye imagen sin errores
- [ ] `make docker-up` — arranca api + bot
- [ ] `make docker-logs` — se ven logs de ambos servicios
- [ ] `make docker-db` — consola SQLite accesible
- [ ] Reiniciar contenedor api → bot reconecta solo

---

## 🔜 Siguiente sesión

1. **Ejecutar checklist de pruebas** → documentar bugs en `docs/diarios/YYYY-MM-DD.md`
2. **Corregir bugs encontrados** (si los hay)
3. **F9.8 — Multi-usuario** solo cuando el checklist esté limpio

---

## 💡 Notas técnicas para recordar

- `main.py` **no ha cambiado** — los imports siguen funcionando igual vía `src.bot.handlers.__init__`
- La imagen Docker usa usuario no-root `thdora:thdora` — el volumen `/app/data` tiene que tener permisos correctos
- `pytest-asyncio==1.3.0` ya está en `requirements.txt` — no hace falta instalar nada extra para los tests
- El `pytest.ini` tiene `asyncio_mode = auto` — no hace falta el decorador `@pytest.mark.asyncio` en cada test
