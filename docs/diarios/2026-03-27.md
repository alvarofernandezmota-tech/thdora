# 📅 Diario thdora — Viernes 27 marzo 2026

**Fase activa:** F7 — Bot Telegram  
**Sesión:** 18:13–18:37 CET

---

## 🎯 Objetivo del día

Setup completo del entorno Ubuntu para F7 + auditoría del repo.

---

## ✅ Logros

### Setup entorno Ubuntu WSL2
- Localizado repo en `~/projects/thdora` (no en `~/proyectos/`)
- Creado venv Python 3.10: `python3 -m venv .venv`
- Instaladas todas las deps con `pip install` directo (setuptools incompatible con `pip install -e`)
- `python-telegram-bot 22.7` ✅
- `fastapi 0.135.2` ✅
- `pytest 9.0.2` ✅
- `git pull` — repo sincronizado con GitHub (17 archivos actualizados)

### Actualizaciones repo
- `pyproject.toml` — `requires-python` bajado a `>=3.10` ✅
- `entorno-local.md` — sección 5 corregida con comando pip real ✅
- Decisión arquitectura IA documentada: **Groq para trivial / Ollama local para datos personales**

### Auditoría F7
- `src/bot/__init__.py` — comandos planificados ✅
- `src/bot/api_client.py` — ❌ falta crear
- `src/bot/handlers.py` — ❌ falta crear
- `src/bot/main.py` — ❌ falta crear
- `.env` — token de Telegram ya emparejado ✅

---

## ⏭️ Siguiente sesión

1. Crear `src/bot/api_client.py` — wrapper httpx → `localhost:8000`
2. Crear `src/bot/handlers.py` — 6 comandos
3. Crear `src/bot/main.py` — arrancar bot
4. Test básico de integración

---

_Creado: 27 marzo 2026 18:37 CET_
