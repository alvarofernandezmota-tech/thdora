# 📓 Diario THDORA — 27 mayo 2026 (S19)

> **Sesión:** ~20:45 CEST (planificación y auditoría)  
> **Última sesión:** 29 abril 2026 (28 días sin sesión)  
> **Versión actual:** v0.16.3

---

## 📋 Estado al inicio de sesión

| Componente | Estado | Nota |
|-----------|--------|------|
| Bot Telegram | ⚠️ Sin verificar | No se ha arrancado desde 29-abr |
| API FastAPI | ⚠️ Sin verificar | Pendiente confirmar endpoints |
| Scheduler APScheduler | ⚠️ Sin verificar | Pend. ver logs en Acer |
| NLP / Groq | 🔴 Bloqueado | GROQ_API_KEY expirada desde 29-abr |
| BBDD SQLite | ⚠️ Sin visualizar | Existe en `src/db/` pero nunca abierta en DBeaver |
| Tests unitarios | ✅ 9 tests | Pasaban al cierre de 29-abr |
| Tests e2e | ❌ Vacíos | Carpeta `tests/e2e/` vacía |
| Docker / CI | ✅ Ok | Actions pasando en 29-abr |

---

## 🎯 Objetivo de la sesión S19

Sesión de **revisión, planificación y decisión arquitectural** antes de tocar código.
No hay commits de código hoy — solo el diario y la hoja de ruta clara.

### Decisiones tomadas hoy

1. **BBDD:** Usar SQLite ahora (ya existe en `src/db/`), conectarla en DBeaver, migrar a Postgres con F10.
2. **Arquitectura multicanal:** FastAPI es el núcleo. Telegram es solo una interfaz. La misma API puede dar servicio a WhatsApp (Twilio), web app, o voz (Whisper) en el futuro sin reescribir lógica.
3. **Orden correcto:** Base sólida primero. Cero features nuevas hasta tener bot verificado, BBDD visible, tests funcionando.
4. **Bot Telegram primero:** Un canal perfecto antes de pensar en otros canales.

---

## 🔍 Deuda técnica inventariada

### Bugs pendientes

| Bug | Issue | Fix | Urgencia |
|-----|-------|-----|----------|
| `/config` timeout | [#10](https://github.com/alvarofernandezmota-tech/thdora/issues/10) | `asyncio.wait_for(..., timeout=5.0)` en `config.py` — 3 líneas | 🔴 ALTA |
| GROQ_API_KEY expirada | — | Renovar en console.groq.com + `docker compose restart bot` | 🔴 ALTA |
| `cb_hab_add_value` importado pero no registrado en `main.py` | — | Auditar handlers registrados | 🟡 MEDIA |
| Cita `id:4` sin `time` en BBDD | — | Limpiar dato sucio en SQLite vía DBeaver | 🟡 MEDIA |

### Documentación pendiente (plan 29-abr)

| Fase | Contenido | Estado |
|------|-----------|--------|
| Fase 3 | Docstrings `bot/` — 15 archivos, empezar por `groq_router.py` (26 KB) | ❌ Pendiente |
| Fase 4 | Docstrings `src/api/` y `src/db/` | ❌ Pendiente |
| Fase 5 | Limpieza `docs/` — borrar ECOSISTEMA.md, mover CLASES_BEGO + GUIA_BEGO | ❌ Pendiente |
| Fase 6 | Auditar Dockerfile, docker-compose, pyproject.toml, .env.example | ❌ Pendiente |
| Fase 7 | Tests integration (14 endpoints) + tests e2e (flujo /nueva) | ❌ Pendiente |

---

## 🏗️ Plan de profesionalización — S19 en adelante

```
FASE 0 (hoy S19)    → Renovar Groq key + Fix bug #10 + Abrir DBeaver
FASE 1 (S20)        → Verificar bot comando a comando (checklist completo)
FASE 2 (S20/S21)    → Limpiar BBDD en DBeaver (dato cita sin time)
FASE 3 (S21-S22)    → Docstrings bot/ (15 archivos) — empezar groq_router.py
FASE 4 (S22-S23)    → Docstrings API + DB
FASE 5 (S23)        → Limpieza docs/ (Fase 5 plan 29-abr)
FASE 6 (S24)        → Tests integration + e2e mínimos
FASE 7 (S25+)       → F10 Multi-usuario sobre SQLite → luego Postgres
FASE 8 (S26+)       → Features avanzadas: F11 scheduler, F12 NLP, F13, F14, F15, F16
```

### Arquitectura multicanal (visión futura)

```
FastAPI (lógica central)
    ├── Bot Telegram (python-telegram-bot)   ← actual — perfeccionar primero
    ├── WhatsApp (Twilio)                    ← futuro F14
    ├── Web app                              ← futuro
    └── Voz (Whisper + Groq)                 ← futuro F15
```

---

## ✅ Checklist de verificación bot — ejecutar en S20

### Arrancar sistema
```bash
# En Acer:
docker compose up -d
docker compose logs -f bot    # buscar errores de arranque
docker compose logs -f api    # verificar que la API responde
```

### Comandos a probar uno a uno
```
[ ] /start              → mensaje de bienvenida + fecha
[ ] /nueva              → flujo completo hasta guardar cita
[ ] /nueva → Tarde      → emoji 🌆 correcto (Bug B1)
[ ] /nueva → Tarde      → 16:00 → "Ver cuartos" (Bug B6)
[ ] /nueva → Noche      → separador Madrugada aparece (Bug B-NEW6)
[ ] /citas              → lista citas hoy
[ ] /citas → navegar ◀️▶️   → cambio de día sin error
[ ] /citas → 🗑️ cita    → DEBE mostrar nombre+hora antes de confirmar
[ ] /habitos            → registrar hábito
[ ] /habitos nombre >15 chars → truncado correcto (Bug B-NEW3)
[ ] /config             → NO se queda cargando (Bug #10 — fix pendiente)
[ ] /semana             → vista 7 días, botones funcionales
[ ] Texto libre NLP     → "mañana dentista a las 10" (requiere Groq key nueva)
[ ] Scheduler           → esperar 20 min → ver log cita próxima en logs
```

### API — endpoints a probar
```bash
curl http://localhost:8000/health
curl http://localhost:8000/appointments
curl http://localhost:8000/habits
curl http://localhost:8000/summary/today
```

### BBDD en DBeaver
```
1. File → New → Database Connection → SQLite
2. Buscar archivo .sqlite (probablemente en data/ o raíz del proyecto)
3. Verificar tablas: appointments, habits, habit_logs, user_config
4. SELECT * FROM appointments LIMIT 20;       ← buscar cita id:4 sin time
5. SELECT * FROM habit_logs ORDER BY date DESC LIMIT 20;
6. Limpiar dato sucio: DELETE o UPDATE cita id:4
```

---

## 🐛 Fix Bug #10 — listo para aplicar

**Archivo:** `src/bot/handlers/config.py`  
**Función:** `_show_hab_configs`

```python
# Añadir al inicio del fichero:
import asyncio

# Cambiar el try/except en _show_hab_configs:
async def _show_hab_configs(query) -> int:
    try:
        configs = await asyncio.wait_for(api.get_all_habit_configs(), timeout=5.0)
    except asyncio.TimeoutError:
        configs = []
    except Exception:
        configs = []
```

**Commit sugerido:** `fix: add timeout to get_all_habit_configs in /config handler (#10)`

---

## 📊 Resumen de la sesión

| Métrica | Valor |
|---------|-------|
| Código modificado | 0 líneas (sesión planificación) |
| Decisiones tomadas | 4 |
| Diario creado y actualizado | ✅ este archivo |
| Plan definido | ✅ 8 fases ordenadas |
| Próxima acción | Renovar Groq key + fix bug #10 + DBeaver |

---

## 📌 Prompt para reanudación (S20)

> Copia esto al inicio de la siguiente conversación:

```
Soy Álvaro. Estamos desarrollando THDORA, un bot de Telegram personal
escrito en Python (python-telegram-bot v20, FastAPI, SQLAlchemy, SQLite, Groq NLP).
Repo: github.com/alvarofernandezmota-tech/thdora

Sesión anterior (27-may-2026, S19):
- Sesión de planificación. Cero código.
- Decisión: BBDD SQLite existente en src/db/ — conectarla en DBeaver
- Decisión: FastAPI como núcleo, Telegram solo como interfaz
- Groq key sigue expirada — pendiente renovar
- Bug #10 /config timeout: fix documentado (3 líneas en config.py)
- Versión actual: v0.16.3 (main, sin cambios desde 29-abr)

Tareas para hoy (S20) por orden:
1. Renovar GROQ_API_KEY en console.groq.com → docker compose restart bot
2. Aplicar fix Bug #10 (ver docs/diarios/2026-05-27.md seción Fix Bug #10)
3. Conectar DBeaver a SQLite → ver datos reales
4. Ejecutar checklist de verificación del bot (ver diario 2026-05-27.md)
5. Abrir issues para lo que falle

Especificación completa en docs/diarios/2026-05-27.md
```

---

_Diario generado al cierre de sesión S19 — 27 mayo 2026 20:55 CEST_
