# 📓 Diario THDORA — 27 mayo 2026 (S19)

> **Sesión:** 20:45–21:15 CEST (planificación + auditoría completa + validación Gemini)  
> **Última sesión:** 29 abril 2026 (28 días sin sesión)  
> **Versión actual:** v0.16.3  
> **Estado al cierre:** Ladrillo vivo — arquitectura sana, bloqueado únicamente por Groq key

---

## 🧐 Tesis — El veredicto final

> **THDORA no está roto. Lleva 28 días dormido por una sola causa: la `GROQ_API_KEY` está expirada.**
> La arquitectura está sana, los dos “fuegos críticos” de Gemini eran falsos positivos,
> y la única bomba real está en la BBDD (cita `id:4` con `time = NULL`).

### Validación cruzada con Gemini

| Diagnóstico Gemini | Auditoría real en repo | Veredicto |
|--------------------|------------------------|----------|
| Bug #10 `/config` timeout — pendiente | `asyncio.wait_for` ya aplicado en `config.py` | ✅ Falso positivo |
| `cb_hab_add_value` no registrado | Se llama desde `_route_free_text()` intencionalmente | ✅ Falso positivo |
| Cita `id:4` sin `time` en BBDD | Confirmado. `nullable=False` en modelo → bomba | 🔴 Real. Limpiar primero |
| `src/core/`, `src/ai/` inexplorados | Auditados. `src/core/` = legacy v1. `src/ai/` = limpio | ✅ Resuelto |
| Groq Tool Calling nativo > LangChain | Confirmado. `src/ai/` ya tiene la separación correcta | ✅ Camino claro |
| Alembic para migraciones | `src/db/migrations/` vacío. Inicializar en S21 | ✅ Pendiente S21 |
| `user_id` como `BigInteger` | Ahora es `String(50)` en `UserConfig`. Fix en migración Postgres | ⚠️ Deuda S25 |

---

## 🔭 Hallazgos clave de la auditoría

| Área | Hallazgo | Veredicto |
|------|----------|-----------|
| `src/bot/main.py` | `cb_hab_add_value` controlado dinámicamente por contexto | ✅ Diseño correcto |
| `src/bot/handlers/config.py` | Bug #10 `asyncio.wait_for` ya aplicado | ✅ Ya corregido |
| `src/core/impl/` | Legacy v1: `json/memory/sqlite_lifemanager.py` | ⚠️ Huérfano, tests lo cubren |
| `src/core/interfaces/` | `abstract_lifemanager.py` — Strategy del THDORA v1 | ⚠️ Huérfano |
| `src/core/ocr/` | Vacía, solo `.gitkeep` | ❌ Código fantasma |
| `src/ai/` | `groq_client.py` + `intent_parser.py` bien separados | ✅ Listo para Tool Calling |
| `src/db/models.py` | `Appointment.time nullable=False` pero `id:4` tiene NULL | 🔴 Limpiar antes de S20 |
| `src/db/migrations/` | Carpeta vacía, Alembic no inicializado | ⚠️ Pendiente S21 |

### El legado src/core/ — patrón Strategy del THDORA v1

```
src/core/  ← no importado por bot ni API actual
    interfaces/abstract_lifemanager.py   ← interfaz Strategy
    impl/json_lifemanager.py             ← backend JSON (descartado)
    impl/memory_lifemanager.py           ← backend memoria (lo usan tests)
    impl/sqlite_lifemanager.py           ← SQLite directo (reemplazado por FastAPI)
    ocr/                                 ← vacía
```

Decisión S19: no se borra. Tests lo cubren. Se evalúa en S21.

### src/ai/ — la capa AI está lista

```
src/ai/
    groq_client.py    ← wrapper Groq async + timeout + retry
    intent_parser.py  ← parsea JSON de Groq → intent + entidades
```

Para Groq Tool Calling: solo modificar `intent_parser.py` añadiendo `tools=[]`
en la llamada. Sin reestructurar nada más.

---

## 📋 Estado al cierre de S19

| Componente | Estado |
|-----------|--------|
| Bot Telegram | ⚠️ Sin verificar (28 días) |
| API FastAPI | ⚠️ Sin verificar |
| NLP / Groq | 🔴 Bloqueado — key expirada |
| BBDD SQLite | 🔴 Cita `id:4` con `time=NULL` |
| Tests unitarios | ✅ 9 tests pasando |
| Docker / CI | ✅ Actions OK |
| src/core/ | ⚠️ Legacy huérfano |
| src/ai/ | ✅ Limpio, listo |

---

## 🚀 Plan de reactivación — S20, ~1 hora

### PASO 1 — Renovar Groq key (5 min)
```bash
# https://console.groq.com → API Keys → Create API Key
cd ~/thdora
nano .env              # GROQ_API_KEY=gsk_nueva_key
docker compose restart bot
docker compose logs -f bot   # buscar "✅ API de THDORA disponible"
```

### PASO 2 — Limpiar BBDD en DBeaver (10 min)
```sql
SELECT id, date, time, name FROM appointments WHERE time IS NULL OR time = '';
DELETE FROM appointments WHERE id = 4;   -- si es dato de prueba
-- O: UPDATE appointments SET time = '00:00' WHERE id = 4;
COMMIT;
```

### PASO 3 — Checklist bot (30 min)
```
[ ] /start              → saludo + fecha
[ ] /nueva → Tarde      → emoji 🌆 (B1)
[ ] /nueva → Tarde      → 16:00 → "Ver cuartos" (B6)
[ ] /nueva → Noche      → separador Madrugada (B-NEW6)
[ ] /citas → navegar ◀️▶️
[ ] /citas → 🗑️          → nombre+hora antes de confirmar
[ ] /habitos            → registrar
[ ] /habitos nombre >15 → truncado (B-NEW3)
[ ] /config             → NO cargando infinito
[ ] /semana             → 7 días OK
[ ] NLP libre           → "mañana dentista a las 10"
[ ] curl localhost:8000/health → 200 OK
```

### PASO 4 — Issues por cada fallo (10 min)
```
Título: [BUG] descripción exacta
Label: bug
Body: pasos para reproducir + esperado vs real
```

---

## 🛣️ Roadmap post-reactivación

```
S20  → Reactivación completa (4 pasos arriba)
S21  → Alembic init + primera migration        ← BASE DE TODO LO QUE VIENE
S21  → Auditar src/core/ legacy (borrar o mantener)
S22  → Groq Tool Calling en intent_parser.py
S22  → Docstrings groq_router.py (26 KB)
S23  → Docstrings resto bot/ (14 archivos)
S23  → Limpieza docs/ (ECOSISTEMA.md, CLASES_BEGO, GUIA_BEGO)
S24  → Tests e2e con AsyncMock (flujo /nueva)
S25  → Multi-usuario: user_id BigInteger FK
S25  → SQLite → Postgres via Alembic
```

### Decisión pendiente para S21

> Gemini pregunta: **¿Alembic primero o groq_router.py primero?**
>
> Respuesta: **Alembic primero.** Es la base que desbloquea todo lo demás:
> sin migraciones controladas no puedes tocar el esquema de BBDD con garantías,
> y multi-usuario requiere alterar tablas. groq_router.py son docstrings
> — no desbloquea nada funcional. Alembic sí.

---

## 📌 Prompt para S20 (copiar y pegar al arrancar)

```
Soy Álvaro. Proyecto: THDORA — bot Telegram personal.
Stack: python-telegram-bot v20, FastAPI, SQLAlchemy, SQLite, Groq NLP, APScheduler, Docker.
Repo: github.com/alvarofernandezmota-tech/thdora

S19 (27-may): auditoría completa. Veredicto: el bot está bien, solo dormído.

Hallazgos confirmados:
- Bug #10 ya corregido. cb_hab_add_value no es bug. src/ai/ limpio.
- src/core/ = legacy v1, no conectado, tests lo cubren.
- Cita id:4 con time=NULL → limpiar ANTES de arrancar.
- Único bloqueador real: GROQ_API_KEY expirada.
- S21 decidido: Alembic primero, luego groq_router.py.

Tareas S20:
1. Renovar GROQ_API_KEY → docker compose restart bot
2. DBeaver → limpiar cita id:4 (time NULL)
3. Ejecutar checklist completo
4. Abrir issues por lo que falle

Diario completo: docs/diarios/2026-05-27.md
```

---

_S19 cerrada — 27 mayo 2026 21:15 CEST_
